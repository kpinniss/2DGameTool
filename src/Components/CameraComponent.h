#ifndef CAMERA_COMPONENT_H
#define CAMERA_COMPONENT_H

#include <SDL2/SDL.h>
#include <glm/glm.hpp>

struct CameraComponent
{
    // SDL_Rect camera;
    // glm::vec2 offset;
    // bool isFollowing;
    // int entityToFollow;
    CameraComponent()
    {
        // camera = {0,0,800,600};
        // offset = {0,0};
        // isFollowing = false;
        // entityToFollow = 0;
    };
    
};

#endif 